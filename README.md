# AnandTaskManagent v2

Multi-user Supabase-connected task/project/time manager.

Replace the files in your existing GitHub repository with the files in this archive. Vercel will redeploy automatically.

The browser uses only the Supabase publishable key; never add a service-role/secret key.