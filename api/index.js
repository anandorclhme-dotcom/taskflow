export default async function handler(request){const u=new URL('/index.html',request.url);const r=await fetch(u);const h=await r.text();return new Response(h.replace('</body>','<script src="/goals-ui.js"></script></body>'),{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}})}